services:
  - type: web
    name: your-app-name
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn run:app"
    env:
      - key: DATABASE_URL
        fromDatabase:
          name: app-dev.db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true