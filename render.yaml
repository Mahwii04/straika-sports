services:
  - type: web
    name: straika-sports
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn run:app"
    env:
      - key: DATABASE_URL
        fromDatabase:
          name: app.db
          property: connectionString
      - key: FLASK_APP
        value: "run.py"